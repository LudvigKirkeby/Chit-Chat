syntax = "proto3";
option go_package = "handin-3/grpc/grpcProto"; // Path to proto file

message Message {
    int64 lamport_clock = 1;
    string text = 2;
}


service MessageService {
    rpc Join(stream Message) returns (stream Message) {} // bidirectional
}
