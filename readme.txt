Instructions to run the program:

1) In the terminal, cd to /server and run the command "go run serverMain.go"
2) In a separate terminal, cd to /client and run "go run clientMain.go"
3) Now, you can begin writing messages as Client 1

In order to add more clients, simply repeat step 2. Messages should be broadcasted to all active clients.